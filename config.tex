\setlength{\parindent}{1cm}
% Controls the indentation of euqations in align* env
\setlength{\mathindent}{0pt}
% Tweak the space between columns for tabular env
\setlength{\tabcolsep}{10pt}
% Set the space between rows for tabular env
\renewcommand{\arraystretch}{1.2}

\setcounter{secnumdepth}{1}
\setcounter{tocdepth}{1}

\renewcommand{\labelenumi}{\textit{\textbf{\alph{enumi}.}}}
% Specify the font size for tabular env
\let\oldtabular\tabular
\renewcommand{\tabular}{\normalsize\oldtabular}
